# Pagecore file-based content showcase

This page demonstrates how Pagecore wires a normal PHP template to editable Markdown files, generated post indexes, uploads, and featured images without introducing a database.

Editors can change this hero copy inline, update post metadata from the post editor, and keep all changes as plain files that can be backed up or versioned.
